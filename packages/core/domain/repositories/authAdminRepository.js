/**
 * Supabase Auth'un kendisi (auth.users tablosu) üzerinde admin
 * yetkisiyle işlem yapan sözleşme. `service role` gerektirir.
 *
 * @typedef {object} AuthAdminRepository
 * @property {(userId: string) => Promise<void>} deleteUser
 */

export {};
